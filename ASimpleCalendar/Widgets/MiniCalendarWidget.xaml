<Window x:Class="ASimpleCalendar.Widgets.MiniCalendarWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="300"
        Height="330"
        WindowStartupLocation="Manual">
    <Border CornerRadius="14"
            Background="#E01C1C1C"
            BorderBrush="#33FFFFFF"
            BorderThickness="1"
            MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock x:Name="MonthText"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="White"
                           VerticalAlignment="Center" />
                <Button Content="×"
                        Width="24"
                        Height="24"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#CCFFFFFF"
                        FontSize="14"
                        Cursor="Hand"
                        Click="Close_Click" />
            </Grid>

            <Grid Grid.Row="1" Margin="0,10,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Пн" Foreground="#88FFFFFF" FontSize="11" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="Вт" Foreground="#88FFFFFF" FontSize="11" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="Ср" Foreground="#88FFFFFF" FontSize="11" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="3" Text="Чт" Foreground="#88FFFFFF" FontSize="11" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="4" Text="Пт" Foreground="#88FFFFFF" FontSize="11" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="5" Text="Сб" Foreground="#88FFFFFF" FontSize="11" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="6" Text="Вс" Foreground="#88FFFFFF" FontSize="11" HorizontalAlignment="Center" />
            </Grid>

            <UniformGrid x:Name="DayGrid" Grid.Row="2" Columns="7" />

            <Thumb Width="14"
                   Height="14"
                   Grid.RowSpan="3"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Cursor="SizeNWSE"
                   Background="Transparent"
                   DragDelta="ResizeGrip_DragDelta" />
        </Grid>
    </Border>
</Window>
